# خطة تحسين وجيز — فحص أمني ومنطقي وواجهات

فحص مباشر للكود بتاريخ 2026-08-03. كل بند مرفق بموضعه في الملف.
الحالة العامة: البنية سليمة ومُحصّنة في أماكن كثيرة (هوية السوكت من التوكن، تعقيم NoSQL عند المُحلّل،
سلاسل ذرّية على المحفظة والكوبونات، `safeUploadName`). المشاكل المتبقية مركّزة في: المصادقة، سباقات
المال، ثغرات XSS مخزّنة في لوحة الإدارة، وفجوة اختبارات كاملة على المسارات.

---

## المرحلة 0 — حرِج، يُنفَّذ فوراً

### 0.1 توليد أكواد OTP والاستعادة بـ `Math.random()`
`routes/auth.js:110, 184, 275, 525, 582, 898` — كل كود تفعيل واستعادة كلمة مرور مولّد بـ
`Math.floor(100000 + Math.random() * 900000)`. مولّد V8 (xorshift128+) يمكن استنتاج حالته الداخلية
من عدد قليل من المخرجات، وأكواد الاستعادة تُطلب علناً بلا حساب. النتيجة: تنبّؤ بكود استعادة أي حساب.

**الإصلاح:** `crypto.randomInt(100000, 1000000)` في دالة واحدة `utils/otp.js` تُستدعى من المواضع الستة.
نفس الشيء لـ `routes/referral.js:51,157` و `models/Marketer.js:8`.

### 0.2 XSS مخزّن في لوحة الإدارة عبر نص الشكاوى
`public_html/admin-complaints.html:234, 239` — `init.innerHTML = \`${c.description}...\`` و
`div.innerHTML = \`${r.message}...\`` بلا هروب. النص يكتبه العميل في `client-complaint.html`.
عميل يرسل شكوى فيها `<img src=x onerror=...>` ينفّذ كوداً في جلسة الأدمن ومعه `adminToken` في
localStorage ⇒ استيلاء كامل على اللوحة.

**الإصلاح:** `window.escapeHtml` موجودة في `public_html/js/config.js:109` — تُطبَّق على الحقلين.
ثم مسح شامل لكل `innerHTML` بمتغيّر في صفحات الإدارة. ونفس البند في
`public_html/client-complaint.html:338` (`subject`).

### 0.3 بوابة الأجهزة الموثوقة للأدمن المساعد صورية
`routes/admin/auth.js:131-138` — عند جهاز غير موثوق يُرجع السيرفر `202 requiresApproval` **ومعه
التوكن الكامل الصالح 7 أيام**. أي مهاجم يعرف كلمة مرور أدمن مساعد يتجاهل شاشة الانتظار ويستخدم
التوكن مباشرةً. الضابط الأمني كله واجهة فقط.

**الإصلاح:** لا يُرسَل توكن مع 202 إطلاقاً. يُحفظ في `SessionRequest.tempToken` (وهو محفوظ فعلاً،
سطر 125) ويُسلَّم فقط عبر `GET /session-requests/:id` بعد `status === 'approved'` ومرة واحدة.
وبنفس المناسبة: `routes/admin/auth.js:174` غير محمي ويكشف حالة أي طلب بالمعرّف — يُقيَّد بـ
`deviceId` مطابق في الجسم.

### 0.4 لا إبطال للتوكنات
لا يوجد `tokenVersion` في `models/User.js`. تغيير كلمة المرور (`routes/auth.js:699`) لا يُبطل
الجلسات القائمة، ولا يوجد مسار خروج فعلي، و `POST /refresh` (`routes/auth.js:715`) يُجدّد بلا سقف
مطلق ⇒ جلسة أبدية من توكن مسروق واحد.

**الإصلاح:** حقل `tokenVersion: Number` في `User`، يُدرَج في الـ JWT، ويُقارَن في
`middleware/authMiddleware.js:51`. يُزاد عند: تغيير كلمة المرور، حذف الحساب، إيقاف الحساب من الإدارة،
وزر "تسجيل الخروج من كل الأجهزة".

### 0.5 قبول التوكن من جسم الطلب على كل المسارات
`middleware/authMiddleware.js:17` — `req.body._token` مقبول عالمياً، بينما الغرض منه
`navigator.sendBeacon` فقط (`routes/beacon.js`). يوسّع سطح الهجوم بلا داعٍ (استغلال عبر نموذج
cross-origin بلا preflight).

**الإصلاح:** حصره في راوتر `/api/beacon` عبر وسيط مخصّص.

### 0.6 `email_verified` غير مفحوص في دخول Google
`routes/auth.js:749` و `815` — يُؤخذ `email` من الـ payload بلا التحقق من `payload.email_verified`.
هوية Google غير مؤكَّدة البريد تُطابق حساباً موجوداً بنفس البريد ⇒ استيلاء على الحساب.

**الإصلاح:** رفض الطلب إذا `email_verified !== true`.

---

## المرحلة 1 — أخطاء منطقية تمسّ المال

### 1.1 سباق يمحو عمولة عند اعتماد سداد
`routes/admin/finance.js:386-408` — بعد `$inc` ذرّي، يُعدَّل المستند المُعاد في الذاكرة
(`captain.wallet_balance = 0`) ثم `captain.save()`. لو خصمت عملية تسليم عمولة في تلك النافذة،
يمسحها الحفظ. المال يضيع بصمت.

**الإصلاح:** كل التعديل في تحديث تجميعي واحد:
`[{ $set: { wallet_balance: { $min: [0, { $add: ['$wallet_balance', amount] }] } } }]` ثم
`is_blocked` بشرط `$expr` في نفس الأنبوب. نفس النمط المُطبَّق أصلاً في `adjust-debt` (سطر 64) —
يُنسَخ هنا.

### 1.2 مبلغ السداد المُبلَّغ للكابتن غير حقيقي
`routes/admin/finance.js:411` — `actualDeduction = requestedAmount` حتى عندما يُقصَّ الرصيد عند 0
(سطر 394). الكابتن يُشعَر بـ "خُصم 5000" وقد خُصم 3200 فعلياً، والفرق يختفي بلا قيد.

**الإصلاح:** `actualDeduction = prevBalanceAfterInc - prevBalance` محسوباً من قيم القاعدة، وقيد
`DebtAdjustment` صريح لأي فائض بدل ابتلاعه.

### 1.3 كوبون طلب التوصيل يُستهلك بلا خصم
`routes/orders.js:166-219` يتحقق من الكوبون ويحسب `serverDiscount` ويخزّنه، و`routes/orders.js:315`
يزيد `usedCount` ويسجّل الاستخدام على المستخدم — لكن `price` و`appFee` لا يُخصم منهما شيئاً
(`orders.js:149-150, 210`)، ولا يُطرح الخصم في أي مكان لاحق. العميل يحرق كوبونه مقابل صفر.
(حالياً غير مرئي لأن `client-order.html` لا يحوي واجهة كوبون أصلاً — الكوبون يعمل في مسار المتجر
فقط عبر `routes/merchant.js:875-1020`.)

**الإصلاح:** قرار صريح — إمّا تطبيق الخصم على `price` قبل حساب `appFee` مع تعديل فحص الحد الأدنى
ليقارن السعر **قبل** الخصم، أو حذف كتلة الكوبون من `POST /api/orders` حتى تُبنى الواجهة. ترك الحالة
الحالية أسوأ الخيارات.

### 1.4 لا إثبات تسليم
`routes/orders.js:1787` — الكابتن يُعلن التسليم من أي مكان وفي أي وقت، تُخصم العمولة وتُغلق
الطلبية. لا فحص قرب من نقطة التسليم رغم وجود `utils/geofence.js` جاهزاً، ولا رمز تأكيد من العميل.

**الإصلاح:** إمّا فحص مسافة (`haversine` ضد `dropoff.lat/lng` بسقف قابل للضبط من `Settings`)،
أو رمز تسليم من 4 خانات يُعرَض للعميل. الأول أخفّ على تجربة الاستخدام.

### 1.5 متغيّر عالمي في مسار التسجيل
`routes/auth.js:106` و `119` — `user = await User.findOne(...)` بلا `let/const` في وحدة CommonJS
غير صارمة ⇒ متغيّر على `global`. طلبا تسجيل متزامنان يتشاركان نفس المرجع؛ النتيجة أخطاء تسجيل
متقطّعة يصعب تفسيرها.

**الإصلاح:** `let user`، وإضافة `'use strict'` أعلى الملفات لمنع تكرارها.

### 1.6 تسريب بيانات عميل عبر الطلبات المعلّقة
`routes/orders.js:2283` — أي كابتن يرى تفاصيل أي طلب `pending` بما فيها اسم العميل وهاتفه، بلا
فحص أن الطلب في مدينته. `requireCity` مُطبَّق على القائمة (`orders.js:903`) وليس على `GET /:id`.

**الإصلاح:** إضافة `order.city === req.user.city` إلى الشرط.

### 1.7 دخول المسوّق بلا تحديد معدّل
`routes/referral.js:72` — `marketer-login` بهاتف + كود من 4 محارف (32⁴)، بلا `rateLimit`، ويعيد
أسماء وهواتف المتاجر المُحالة. قابل للتخمين آلياً. و`/register` (سطر 32) بلا حدّ ⇒ إغراق.

**الإصلاح:** `rateLimit` على الاثنين، ورفع طول الكود إلى 6 محارف.

### 1.8 تعداد الحسابات في الاستعادة
`routes/auth.js:683` يُرجع `404 الحساب غير موجود`، و`forgot-password` يميّز بين رسالتين
(سطر 578 مقابل 597/608). كلاهما يكشف وجود الحساب.

**الإصلاح:** رد موحّد في الحالتين.

---

## المرحلة 2 — تصلّب عام

- **CSP معطّلة كلياً** — `index.js:125` (`contentSecurityPolicy: false`). أهم طبقة دفاع ثانية ضد
  أي XSS متبقٍّ. تُفعَّل بسياسة مبدئية في وضع `report-only`، ثم تُشدَّد بعد إزالة الـ inline scripts.
- **`origin === 'null'` مقبول في CORS** — `index.js:108` و`189`. `null` هو ما ترسله صفحة في
  `iframe sandbox` — أي موقع يستطيع انتحاله. تُستبدل بقائمة مخططات Capacitor الصريحة الموجودة أصلاً.
- **قوة كلمة المرور 6 محارف بلا أي شرط** — `schemas/authSchema.js:9` و`routes/auth.js:669`.
  الحد الأدنى 8 مع رفض قائمة الأكثر شيوعاً.
- **`/register-captain` بلا مخطط Zod** — `routes/auth.js:163` يقبل حقولاً حرّة بينما `/register`
  مُحقَّق. يُضاف `captainRegisterSchema`.
- **لا حدّ لمحاولات OTP لكل حساب** — الحدّ الحالي على IP فقط (`otpLimiter`). عدّاد
  `otpAttempts` على المستخدم يُبطل الكود بعد 5 محاولات خاطئة.
- **`JWT_SECRET_LEGACY`** — `middleware/authMiddleware.js:39`. يُحدَّد له تاريخ إزالة ويُربط
  بـ `minVersion` في `Settings`.
- **`BOT_API_URL` يعود افتراضياً إلى `http://localhost:3000`** — `routes/auth.js:68` وهو السيرفر
  نفسه. يُترك فارغاً ويُعطَّل المسار عند غيابه.
- **استعلام مستخدم كامل على كل طلب** — `authMiddleware.js:51`. كاش قصير (30 ثانية) مفتاحه
  `userId` يقلّص الحمل على القاعدة بنسبة كبيرة.

---

## المرحلة 3 — الواجهات وتجربة الاستخدام

- **إتاحة (a11y)**: 9 صفحات فقط من 65 تستخدم `aria-label`. الأولوية لمسار الطلب
  (`client-order.html`) ولوحة الكابتن: تسميات على الأزرار الأيقونية، `aria-live` على حالة الطلب،
  تباين ألوان مطابق لـ WCAG AA.
- **`dir="rtl"` ناقص** في `admin-login.html` و`_promo.html` — يكسر محاذاة رسائل الخطأ العربية.
- **`alert()` أصلي في 12 موضعاً** رغم وجود `js/native-dialogs.js`. يُوحَّد على النظام الموجود —
  `alert` يحجب الواجهة ويبدو غريباً داخل تطبيق مغلّف.
- **رسائل الخطأ تُعرض خامّاً**: `${e.message}` في `admin-complaints.html:169,244` وغيرها. تُستبدل
  برسالة عربية موحّدة مع زر "إعادة المحاولة"، وتفصيل الخطأ في الكونسول فقط.
- **حالات فارغة/تحميل**: مسح صفحة بصفحة للتأكد من ثلاث حالات لكل قائمة (تحميل، فارغة، خطأ).
- **`console.log` متبقٍّ** في 11 ملف واجهة رغم `console-cleaner.js` — يُنظَّف عند البناء.

---

## المرحلة 4 — البنية والجودة

- **صفر اختبارات على المسارات**: 273 اختباراً تمرّ كلها، لكنها على `utils/` فقط. `supertest` مثبّت
  وغير مستخدم. الأولوية: `POST /api/orders`، `PUT /:id/deliver`، `payment-requests/:id/approve`،
  و`admin/login` — أي المسارات التي تلمس المال والصلاحيات.
- **`routes/orders.js` = 2400 سطر**. يُقسَّم كما قُسّم `admin.js` سابقاً: `orders/create.js`،
  `orders/lifecycle.js`، `orders/negotiation.js`، `orders/errand.js`.
- **`require()` داخل الدوال** في عشرات المواضع (`orders.js:155, 174, 645, 1830…`). يُرفع لأعلى
  الملف — يخفي التبعيات ويعيق التحليل الساكن.
- **ملفات مؤقتة في الجذر**: `1wajeezsd.zip` (16MB)، `wajeezsd.zip` (12MB)، `base64.txt`،
  `tmp/`، `tmp2/`، `testsprite_tests/`، `* - Copy.*`. مُتجاهَلة في git لكنها تشوّش وتضخّم النسخ
  الاحتياطي. تُحذف.
- **مراقبة**: `/api/health` جيد. تُضاف تنبيهات على `push.ready === false` وعلى معدّل 5xx من
  `utils/errorTracker.js`.

---

## ترتيب التنفيذ المقترح

| # | الدفعة | البنود | الأثر |
|---|--------|--------|-------|
| 1 | أمان حرِج | 0.1 → 0.6 | تُغلق ثلاث طرق استيلاء على حسابات |
| 2 | نزاهة المال | 1.1، 1.2، 1.5 | يوقف ضياع المال والسباقات |
| 3 | قرار الكوبون + إثبات التسليم | 1.3، 1.4 | يُغلق فجوتين تشغيليتين |
| 4 | تصلّب + تسريبات | 1.6 → 1.8، المرحلة 2 | يقلّص سطح الهجوم |
| 5 | اختبارات المسارات | المرحلة 4 (أول بند) | يحمي ما سبق من الانحدار |
| 6 | الواجهات | المرحلة 3 | جودة ملموسة للمستخدم |
| 7 | إعادة الهيكلة | باقي المرحلة 4 | صيانة أسهل |
