# النسخ الاحتياطي والاستعادة

القاعدة إنتاجية وعليها طلبات حقيقية وأرصدة تجّار. هذا الملف هو الإجراء —
لا شرحٌ عامّ.

## النسخ

```bash
npm run backup
```

يكتب في `backups/<التاريخ>Z/` ملفاً لكل مجموعة (`<اسم>.ndjson.gz`) ومعها
`manifest.json` فيه العدد والحجم وبصمة sha256 لكل ملف. يُبقي آخر **14**
نسخة ويحذف ما قبلها.

| الخيار | ماذا يفعل |
|---|---|
| `--dry-run` | يعدّ المستندات ولا يكتب شيئاً — للتأكّد من الاتصال |
| `--only orders,users` | مجموعاتٍ بعينها |
| `--out D:/wajeez-backups` | مجلدٌ آخر (قرصٌ آخر أفضل) |
| `--keep 30` | كم نسخة تُبقى |

**⚠️ الملف الناتج فيه بيانات مستخدمين حقيقية** — أرقام هواتف، عناوين،
بصمات كلمات مرور. عامله معاملة السرّ: `backups/` مستثنىً من المستودع،
ضعه على قرصٍ مشفَّر، ولا يُرسل في محادثة ولا يُرفع لتخزينٍ عام.

## الاستعادة

نسخةٌ لم تُجرَّب استعادتها ليست نسخة. جرّبها كل فترة:

```bash
node scripts/restore-db.js --from backups/2026-09-16T115520Z
```

بلا `--yes` يطبع الخطّة ويفحص البصمات ولا يكتب شيئاً. أضف `--yes`
فيُستعاد إلى قاعدةٍ **جديدة** اسمها `<القاعدة>_restore_<وقت>` بجانب
الحيّة — تفحصها ثم تقرّر. القاعدة الحيّة لا تُمسّ.

### الكتابة فوق القاعدة الحيّة

تحتاج أربعة أشياء معاً، عمداً:

```bash
WAJEEZ_ALLOW_INPLACE_RESTORE=1 node scripts/restore-db.js \
  --from backups/<النسخة> --in-place --yes --confirm-db <اسم القاعدة>
```

نقص أيٍّ منها يعني رفضاً بلا كتابة. وقبل أي كتابة تُفحص بصمة sha256 لكل
ملف: نسخةٌ انقطعت أو تغيّر فيها بايت تُرفض كاملةً.

## الجدولة

الأمر لا يحتاج إلا Node وملف `.env`. على الخادم:

```cron
17 2 * * * cd /path/to/wajeezsd && /usr/bin/node scripts/backup-db.js --quiet >> /var/log/wajeez-backup.log 2>&1
```

وعلى ويندوز عبر Task Scheduler بنفس الأمر.

**النسخة التي تعيش على نفس الخادم ليست نسخة احتياطية.** انسخ مجلد
`backups/` إلى مكانٍ آخر — قرصٍ خارجي أو تخزينٍ بعيد — وإلا ضاعت مع
الخادم الذي تحميه منه.

## لماذا لا `mongodump`

أدوات MongoDB الرسمية ليست مثبَّتة على الخادم، والاعتماد عليها يعني
نسخةً لا تعمل يوم تحتاجها. هذان السكربتان لا يحتاجان إلا ما يحتاجه
التطبيق نفسه.

الصيغة Extended JSON **القانوني** (`relaxed: false`) لا المُرتاح: المُرتاح
يكتب `ObjectId` نصّاً والتاريخ نصّاً، فتعود النسخة بأنواعٍ غير أنواعها —
وهي نسخةٌ تكذب. اختبارات `tests/dbBackup.test.js` تحرس هذا بالذات.
